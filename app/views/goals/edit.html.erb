<% if @goal.errors.any? %>
  <div id="error_explanation">
    <h2>There were some errors:</h2>
    <ul>
      <% @goal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>


<h1> Edit Goal </h1>
<%= form_for @goal do |f| %>
    <%= f.label "Title:" %><br>
    <%= f.text_field :title %><br>
  
    <%= f.label 'Description:' %><br>
    <%= f.text_area :description %><br>

    <%= f.collection_check_boxes :sphere_ids, spheres_helper, :id, :title %> <br>
  
    <%= f.submit %>
  <% end %>